# 税务案头

中国大陆一级市场股权投资税务知识问答的小范围测试前端，直接调用已发布的阿里云百炼知识问答服务。

访问：<https://allenruan92.github.io/tax-law-chat/>

公众号访客请使用独立的[公众号聊天页](https://allenruan92.github.io/tax-law-chat/wechat.html)：从「钻木者得火」的回复取得专属入口，无需填写 Key，没有知识库管理权限。后端接入方式与配置步骤见 [公众号接入说明](wechat/README.md)。原管理页仍保持上述 Key 连接方式。

## 使用

1. 打开网页，点击「连接设置」，填入百炼 API Key。应用 ID 已预设。
2. 提问后可继续追问；页面会携带最近最多 10 轮完整问答，总上下文按 24,000 字符截取。这是前端会话管理，不是长期记忆。
3. 点击「复制体验链接」，私下发给试用同事。同事打开后会导入连接信息，不会收到你的聊天记录。
4. 点击「新建对话」会保留旧记录；从左侧「本机对话」列表切换，点「⋯」可重命名或删除。手机点击顶部「☷」打开历史列表。每段对话有独立上下文与草稿。
5. 对话存于本浏览器的 IndexedDB，刷新、关闭后重新打开仍可读取；不自动同步到同事或其他浏览器/设备。清理站点数据、隐私模式结束或浏览器回收存储可能丢失记录。请用「导出备份」保存全部对话 JSON，「导入备份」追加恢复，不覆盖现有记录；「导出对话」仍导出当前对话 Markdown。

历史版首次打开会迁移此标签页仍保留的旧对话；已经关闭且被浏览器清除的旧记录无法追回。连接 Key 仍仅保存在当前标签页，不写入对话数据库或备份。恢复历史对话不等于恢复 Key，重新打开时可能需要再次填写 Key 或使用体验链接。所有本机对话共用此标签页的连接 Key。

生成回答期间可停止生成，但不能切换、删除或重命名对话。中断回答恢复为「未完成」，不进入后续上下文。同一对话被其他标签页修改或删除后，本页的后续修改会保存为「恢复副本」，避免覆盖。存储不可用或空间不足时会显示警告；请及时导出，不要清理站点数据。对话列表与备份只在本机处理；提问时仍会将问题和近期上下文发送到百炼。

## 当前服务

- 应用：`tax-law-poc_知识问答`
- 应用 ID：`aid-c13de479d8944f12b3c45ef9a45af154`
- 工作空间：`llm-zc86z2f8ixro6odd`
- 接口：`https://llm-zc86z2f8ixro6odd.cn-beijing.maas.aliyuncs.com/api/v2/apps/knowledge/chat`
- 不指定 `agent_version`，调用最新发布版本。

仅显示知识问答接口 `generating` 阶段的正文，不展示规划、工具执行信息。失败或中断的问答不进入后续上下文。初始资料是一份入门概览，可通过知识库管理补充。回答不是税务意见，须核对法规原文、适用条件及有效日期。

## 知识库管理

点击侧栏「知识库管理」（手机上为 ▤ 图标）。管理目标固定为 `tax-law-poc`（`uwohowhkia`），不会随连接设置中的问答应用 ID 改变。

- 选择或拖入文件，点击「上传」。页面依次完成上传凭证申请、文件传输、登记、解析与索引；显示「可检索」后再提问验证。面板打开时每 5 秒自动刷新，支持分页。
- 本页支持 PDF / DOC / DOCX / PPT / PPTX（≤150 MB）、TXT / MD / Markdown / HTML / XLS / XLSX（≤10 MB）、PNG / JPG / JPEG / BMP / GIF（≤20 MB），每批最多 10 个。页数等进一步限制由百炼校验；扫描件的解析质量需自行核对。
- 文件会上传到阿里云百炼数据中心，并加入当前知识库。解析、向量化、存储及后续问答可能产生费用，以账号实际计费为准。文件名建议保留法规日期、文号和有效版本。
- 「停止上传」只中断前端当前操作，不回滚已上传或登记的文件。网络中断后先刷新列表核对，避免重复提交；已登记但未提交入库的任务支持「重试入库（不重传）」。
- 「移除」需要核对文件名、勾选并确认。它删除当前知识库中该文档的索引与切片，影响所有试用者；**数据中心源文件保留**，没有永久删除源文件或整个知识库的入口。同步可能延迟约 30 秒。
- 「下载」保存上传时的原文件（含扩展名），不改变云端文件或索引。每次点击重新获取短期原文件地址，地址不写入本地存储或备份。未完成解析、缺少原文件地址时提示到控制台核对；不会把解析切片当原文件。网页限 150 MB、同时一个下载，支持取消；下载内容大小须与库内记录一致。文件可能包含敏感信息，请按内部制度保存和转发。
- 上传任务的非敏感状态保存在当前标签页，刷新后可继续查询已提交的入库任务；原始文件不保存在浏览器存储。清空任务记录只清本地列表，不删除云端文件。关闭页面会打断尚未完成的传输。

这是共享管理权限的 POC，没有用户角色区分。Key 必须有该工作空间的管理权限，仅能问答的 Key 可能返回 401 / 403。列表、入库和移除由浏览器直连百炼；上传凭证和登记接口受跨域限制，改由本账号的 FC 代理。文件用临时签名地址直接上传 OSS，API Key 不发送给 OSS。

**管理版已接入 FC 上传代理（2026-09-22）**：已完成真实浏览器的上传、解析入库、问答检索及确认移除测试，原始税法资料保留。`lib/config.js` 中仅保存公开代理地址；代理地址为空时禁止上传。部署配置和验收说明见 [fc/README.md](fc/README.md)。

## 分享与费用

这是用户接受共享 Key 方式后制作的验证版本，没有用户账号或角色区分。GitHub Pages 链接可公开访问；API 调用费用由 Key 所属百炼账号承担，按应用实际调用的服务计费。上传管理版使用 FC 小型代理，其函数调用和运行资源另按阿里云实际用量计费，无预置常驻实例。

Key 不写入仓库或静态代码，仅保存在当前标签页的 `sessionStorage` 中。体验链接的 `#access_key=…` 包含 Key，打开后会从地址栏移除；拿到链接或使用网页的人仍可提取 Key。Key 不一定只授权当前应用。不要公开发布体验链接；结束测试请在百炼撤销或更换 Key。网页「清除 Key」只清本地，不会撤销已分享链接。

浏览器直接向百炼发送问题及近期上下文。管理版会将 Key 和上传元数据发送到账号内的 FC 代理，代理仅保存 Key 的摘要用于校验，不记录明文。网页不使用第三方统计、字体或脚本。GitHub Pages 在部分大陆网络下可能不稳定。

## 本地运行与检查

Node.js 20+，无需 npm 依赖：

```sh
npm start
npm test
```

打开 <http://127.0.0.1:4173>。本地预览仅提供前端静态文件，不读取 Key。单元测试覆盖 SSE 分片、阶段过滤、错误、上下文裁剪、历史/备份验证、下载校验和知识库接口协议。

浏览器回归：安装 Playwright 并使用已安装的 Chrome，执行 `node tests/browser.e2e.cjs`。可用 `TAX_PLAYWRIGHT_MODULE` 指向本机 Playwright 模块。测试使用全新隔离浏览器上下文与模拟问答，不接触使用者的浏览器数据；`--live` 验证已部署静态页面。可选 `--download` 从本机 `DASHSCOPE_API_KEY` 读取凭证，仅下载已存在的原始税法文档进行验证，不上传或移除云端文件。测试产物均在忽略的 `output/playwright/`。

## 部署

GitHub Pages 使用 `main` 分支根目录，无需构建。推送后由 GitHub 更新站点。跨域须由百炼接口允许页面的来源；不能靠前端代码绕过。
